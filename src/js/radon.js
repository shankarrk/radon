var radon = !function () {
	const radon = { version: '0.0.1' };

	//setup the application
	function setup(rootCanvas = null) {
		if (rootCanvas) {
			drawScales(rootCanvas);
		}
	}


	//draw the scales
	function drawScales(rootCanvas = null) {
		if (rootCanvas) {
			
		}
	}
	
	//Draw the basic canvas surface
	function drawSurface(rootCanvas = null) {
		if(rootCanvas)
		{
			
		}
	}

	this.radon = radon;
	return this;
} ();